#!/bin/bash
# Invader CLI wrapper
# Usage: ./bin/invade <command> [args]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_DIR" && mix invade "$@"
